# This file was generated using Kotlin DSL (/tmp/it.krzeminski.githubactions.EndToEndTest251588526669498683.tmp).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/krzema12/github-actions-kotlin-dsl

name: Test workflow

on:
  push:

jobs:
  "check_yaml_consistency":
    runs-on: "ubuntu-latest"
    steps:
      - name: Hello world!
        run: echo 'hello from consistency job!'
  "test_job_1":
    runs-on: "ubuntu-latest"
    needs:
      - "check_yaml_consistency"
    steps:
      - name: Hello world!
        run: echo 'hello!'
  "test_job_2":
    runs-on: "ubuntu-latest"
    needs:
      - "test_job_1"
      - "check_yaml_consistency"
    steps:
      - name: Hello world, again!
        run: echo 'hello again!'
