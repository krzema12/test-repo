name: "Test workflow"
on:
  push: {}
jobs:
  "test_job":
    runs-on: "ubuntu-latest"
    steps:
    - 
      uses: "actions/checkout@v2"
      with:
        
        fetch-depth: 0
    - 
      name: "Echo to the world"
      run: "echo 'hello ${{ matrix.testTask }}'"
    needs: []
    strategy:
      matrix:
        "testTask":
        - "pythonTest"
        - "microPythonTest"
  "test_job2":
    runs-on: "ubuntu-latest"
    steps:
    - 
      name: "Some random echo"
      run: "echo 'hello!'"
    needs:
    - "test_job"
    strategy: {}
