name: "Test workflow"
on:
  push: {}
jobs:
  "test_job":
    runs-on: "ubuntu-latest"
    steps:
    - 
      uses: "actions/checkout@v2"
      with:
        
        fetch-depth: 0
    - 
      name: "Install Kotlin for scripting"
      run: "sudo snap install --classic kotlin"
    needs: []
  "test_job2":
    runs-on: "ubuntu-latest"
    steps:
    - 
      name: "Some random echo"
      run: "echo 'hello!'"
    needs:
    - "test_job"
